@model IEnumerable<BussinessObjects.ReactType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ReactType1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ReactType1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReactTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ReactTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ReactTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
